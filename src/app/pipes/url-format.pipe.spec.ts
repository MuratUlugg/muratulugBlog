import { UrlformatPipe } from './url-format.pipe';

describe('UrlformatPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlformatPipe();
    expect(pipe).toBeTruthy();
  });
});
